import React from "react";

function AgentModule() {
  return (
    <div className="cafe-placeholder">
      <h4>Agent Module (TODO)</h4>
      <p className="cafe-note">该模块预留用于 skill 列表、调用编排和自动化分析模板。</p>
      <p className="cafe-note">当前阶段仅保留模块壳，不接入宿主内部状态。</p>
    </div>
  );
}

export default AgentModule;
